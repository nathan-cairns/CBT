#!/usr/bin/env python3

from setuptools import setup

setup(name='cbt',
      description='Generate code using machine learning',
      author='Buster & Nathan',
      packages= ['cbt'],
      install_requires=[
            'staticfg'
      ])
